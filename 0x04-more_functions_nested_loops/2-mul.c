#include "main.h"
#include <stdio.h>
/**
 * mul - multiplies two integers
 * @y: first integer to be multiplied
 * @x: the second integer to be multplied with first integet
 * Return: results of multplying the two integers
*/
int mul(int y, int x)
{
	return (y * x);

}
